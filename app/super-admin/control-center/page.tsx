import { AdminControlCenter } from "@/components/admin/admin-control-center";

export default function SuperAdminControlCenterPage() {
  return (
    <AdminControlCenter
      title="Super Admin Control Center"
      description="Govern the full application from a separate super-admin surface with the same live registry, CRUD engine, and realtime sync."
    />
  );
}
